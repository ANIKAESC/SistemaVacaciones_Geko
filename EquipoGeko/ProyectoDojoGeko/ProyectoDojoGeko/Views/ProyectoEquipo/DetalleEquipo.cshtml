@*
@model ProyectoDojoGeko.Models.EquipoViewModel
@{
    ViewData["Title"] = "Detalle del Equipo";
    Layout = "~/Views/Shared/_DashboardLayoutSistema.cshtml";
}
<div class="equipo-detalle-container">
    <div class="page-header">
        <div class="header-content">
            <h1 class="page-title">
                <i class="fas fa-users"></i>
                Detalle del Equipo
            </h1>
            <p class="page-subtitle">Información completa del equipo y empleados asignados</p>
        </div>
        <div class="header-actions">
            <a href="@Url.Action("Index", "ProyectoEquipo")" class="btn-back">
                <i class="fas fa-arrow-left"></i>
                <span>Volver</span>
            </a>
            <a href="@Url.Action("EditarEquipo", "ProyectoEquipo", new { id = Model.IdEquipo })" class="btn-edit">
                <i class="fas fa-edit"></i>
                <span>Asigna o elimina empleados para este equipo</span>
            </a>
        </div>
    </div>
    <div class="equipo-card">
        <div class="equipo-header">
            <div class="equipo-icon">
                <i class="fas fa-users"></i>
            </div>
            <div class="equipo-info">
                <h2>@Model.Nombre</h2>
                <div class="equipo-meta">
                    <span class="equipo-id">ID: @Model.IdEquipo</span>
                </div>
            </div>
        </div>
        <div class="equipo-content">
            <div class="info-section">
                <div class="section-header">
                    <h3>
                        <i class="fas fa-info-circle"></i>
                        Información General
                    </h3>
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">
                            <i class="fas fa-align-left"></i>
                            Descripción
                        </div>
                        <div class="info-value">
                            @(string.IsNullOrEmpty(Model.Descripcion) ? "Sin descripción" : Model.Descripcion)
                        </div>
                    </div>
                </div>
            </div>
            <!-- Empleados asignados -->
            <div class="info-section">
                <div class="section-header">
                    <h3>
                        <i class="fas fa-user-friends"></i>
                        Empleados Asignados
                    </h3>
                </div>
                <div class="empleados-list">
                    @if (ViewBag.Empleados != null && ((List<EmpleadoViewModel>)ViewBag.Empleados).Count > 0)
                    {
                        foreach (var empleado in (List<EmpleadoViewModel>)ViewBag.Empleados)
                        {
                            <div class="empleado-item">
                                <div class="empleado-icon">
                                    <i class="fas fa-user"></i>
                                </div>
                                <div class="empleado-info">
                                    <h4>@empleado.NombresEmpleado</h4>
                                    <p>@empleado.Puesto</p>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="empty-state">
                            <i class="fas fa-info-circle"></i>
                            <p>No hay empleados asignados a este equipo</p>
                        </div>
                    }
                </div>
                
            </div>
        </div>
    </div>
</div>
@section Styles {
    <link rel="stylesheet" href="~/css/empleado-detalle.css" />
}
*@



@model ProyectoDojoGeko.Models.EquipoViewModel
@{
    ViewData["Title"] = "Detalle del Equipo";
    Layout = "~/Views/Shared/_DashboardLayoutSistema.cshtml";
}
<div class="proyecto-detalle-container">
    <!-- Header -->
    <div class="page-header">
        <div class="header-content">
            <h1 class="page-title">
                <i class="fas fa-file-alt"></i>
                Detalle del Equipo
            </h1>
            <p class="page-subtitle">Información completa del equipo y empleados asignados</p>
        </div>
        <div class="header-actions">
            <a href="@Url.Action("Index", "ProyectoEquipo")" class="btn-back">
                <i class="fas fa-arrow-left"></i>
                <span>Volver</span>
            </a>
            <a href="@Url.Action("EditarEquipo", "ProyectoEquipo", new { id = Model.IdEquipo })" class="btn-edit">
                <i class="fas fa-edit"></i>
                <span>Asigna o elimina empleados para este equipo</span>
            </a>
        </div>
    </div>

    <!-- Tarjeta principal -->
    <div class="proyecto-card">
        
        <!-- Contenido -->
        <div class="proyecto-content">
            <div class="info-section">
                <div class="section-header">
                    <h3>
                        <i class="fas fa-info-circle"></i>
                        Información General
                    </h3>
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">
                            <i class="fas fa-align-left"></i>
                            <h3>@Model.Nombre</h3>
                        </div>
                        <div class="info-value">
                            ID: @Model.IdEquipo
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">
                            <i class="fas fa-align-left"></i>
                            Descripción
                        </div>
                        <div class="info-value">
                            @(string.IsNullOrEmpty(Model.Descripcion) ? "Sin descripción" : Model.Descripcion)
                        </div>
                    </div>
                </div>
            </div>

            <!-- Equipos asignados -->
            <div class="info-section">
                <div class="section-header">
                    <h3>
                        <i class="fas fa-users"></i>
                        Equipos Asignados
                    </h3>
                </div>
                <div class="equipos-list">
                    @if (ViewBag.Empleados != null && ((List<EmpleadoViewModel>)ViewBag.Empleados).Count > 0)
                    {
                        foreach (var empleado in (List<EmpleadoViewModel>)ViewBag.Empleados)
                        {
                            <div class="empleado-item">
                                <div class="empleado-icon">
                                    <i class="fas fa-user"></i>
                                </div>
                                <div class="empleado-info">
                                    <h4>@empleado.NombresEmpleado</h4>
                                    <p>@empleado.Puesto</p>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="empty-state">
                            <i class="fas fa-info-circle"></i>
                            <p>No hay empleados asignados a este equipo</p>
                        </div>
                    }
                </div>
            </div>
        </div>
        <hr />
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/empleado-detalle.css" />
}

<style>
    .page-header{
        margin: 10px 30px;
    }

    .proyecto-card {
        margin: 30px 30px;
        padding: 2% 2%;
        box-shadow: 0 1px 3px 0 rgba(0 0 0 / .1);
        border-radius: 10px;
    }

    .proyecto-footer {
        display: flex;
        flex-flow: row-reverse;
        background-color: #f9fafb;
        border-radius: 10px;
        margin-top:2%;
        padding:40px  28px;
    }
</style>